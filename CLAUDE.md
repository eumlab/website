# CLAUDE.md

This project's guidance for AI tools lives in **[AGENTS.md](AGENTS.md)** — read it first.

TL;DR: Astro static rebuild of the legacy Django EUMLab site; **every page must stay
CSS/visually identical to the original**, enforced by `npm run verify` (must be 0 diff).
See AGENTS.md for the stack, commands, verification workflow, and gotchas.

Production/server deploy details: `DEPLOYMENT.local.md` (gitignored).

---
// Localized Pro Metronome for Education — /de|fr|es|zh|ja/pro-metronome/edu/.
import ProMetronomeEdu from '../../../../components/pages/ProMetronomeEdu.astro';
import { NON_DEFAULT_LOCALES } from '../../../../i18n/config';
export function getStaticPaths() {
  return NON_DEFAULT_LOCALES.map((l) => ({ params: { lang: l.code } }));
}
const { lang } = Astro.params;
---
<ProMetronomeEdu lang={lang} />
